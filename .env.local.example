# Optional: override where the frontend proxies /api/* requests.
# Defaults to http://localhost:8000 (the local uvicorn backend) if unset.
# Copy this file to ".env.local" and edit if your backend runs elsewhere.
API_PROXY_TARGET=http://localhost:8000
